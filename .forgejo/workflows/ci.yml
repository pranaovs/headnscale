name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  ci:
    name: Format, Test & Build Go
    runs-on: self-hosted

    container:
      image: codeberg.org/pranaovs/go-ci:latest@sha256:cb3b02dd526f2b3ca6fe1b7c52b586ddc95fa4a0563832d557412898e204d3bc

    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      # - name: Setup cache
      #   uses: actions/cache@v5
      #   with:
      #     path: |
      #       /go/pkg/mod
      #       /root/.cache/go-build
      #     key: ${{ runner.os }}-go-${{ hashFiles('server/go.sum') }}
      #     restore-keys: |
      #       ${{ runner.os }}-go-

      - name: Download dependencies
        run: |
          go mod download
          go mod verify

      - name: Check formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "Files not formatted: $unformatted"
            exit 1
          fi
        continue-on-error: true

      - name: Vet code
        run: go vet ./...

      - name: Run tests
        run: go test -v ./...

      - name: Build
        run: go build -v ./...
